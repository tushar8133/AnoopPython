from tkinter.simpledialog import askstring

class Receive:
    def __init__(self):
        # pass
        # tkEntry.place(x=pos['x'], y=pos['y'], width=pos['w'], height=pos['h'])
        # tkSend.place(x=500,y=450,width=66,height=30)
        # # tkEntry.set = ""
        # tkEntry.delete(0, 'end')
        # tkSend.wait_variable(userInput)
        # tkEntry.place(x=30,y=-100,width=450,height=30)
        # tkSend.place(x=500,y=-100,width=66,height=30)
        # return tkEntry.get()

        '''
        # Import the required library
        from tkinter import *
        from tkinter.simpledialog import askstring
        from tkinter.messagebox import showinfo

        # Create an instance of tkinter frame and window
        win=Tk()
        win.geometry("700x300")

        name = askstring('PLEASE ENTER', 'What is your name?')
        showinfo('Hello!', 'Hi, {}'.format(name))

        win.mainloop()
        '''

receive = Receive()